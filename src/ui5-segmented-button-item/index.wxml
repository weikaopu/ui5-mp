<view class="ui5-sb-item {{selected ? 'is-selected' : ''}} {{disabled ? 'is-disabled' : ''}}" bindtap="onTap">
  <ui5-icon wx:if="{{icon}}" src="{{icon}}" size="16" />
  <text wx:if="{{text}}" class="ui5-sb-item__text">{{text}}</text>
</view>
