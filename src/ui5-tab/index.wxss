.ui5-tab-content {
  animation: fadeIn 0.3s ease-in-out;
}

/* 动画过渡 */
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(5px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}