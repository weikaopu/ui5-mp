<view class="ui5-badge ui5-badge--{{colorScheme}}">
  <view wx:if="{{icon}}" class="ui5-badge__icon">
    <ui5-icon name="{{icon}}" size="10" />
  </view>
  <text class="ui5-badge__text">
    <slot></slot>
  </text>
</view>